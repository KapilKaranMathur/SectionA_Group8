Actions Taken
Whitespace Removal: Applied TRIM to all text columns to remove leading/trailing spaces.

Null Handling: * Numerical: Filled missing values in Sale Price and Flat Area using the Median.

Dropping: Deleted rows missing essential info like ID.

Standardization: * Converted Date House was Sold to a uniform Date format.

Standardized text (e.g., "fair", "Fair", "FAIR") to a single consistent case.

Duplicate Removal: Removed redundant rows based on unique IDs.

Data Validation: Ensured all numerical columns (Bedrooms, Bathrooms) contain valid digits only.